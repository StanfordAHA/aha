#!/bin/bash -e
if [[ ${BUILDKITE_STEP_KEY} = "integration-tests" ]]; then
    mkdir -p $BUILDKITE_BUILD_CHECKOUT_PATH/temp
    rm -rf $BUILDKITE_BUILD_CHECKOUT_PATH/temp/.TEST
    touch $BUILDKITE_BUILD_CHECKOUT_PATH/temp/.TEST
fi